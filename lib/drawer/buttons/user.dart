import 'package:flutter/material.dart';

class UserButtonNav extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return IconButton(
      icon: Icon(Icons.account_box, color: Colors.white,),
      tooltip: 'Usuario',
      onPressed: (){},
    );
  }

}