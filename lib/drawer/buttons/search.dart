import 'package:flutter/material.dart';

class SearchButtonNav extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return IconButton(
      icon: Icon(Icons.search, color: Colors.white,),
      tooltip: 'Búsqueda',
      onPressed: (){}
    );
  }

}